using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApplication1
{


    class Program
    {
        static float diff(int a, int b)
        {
            return Math.Abs(a - b);
        }
        static void Main(string[] args)
        {
            /*
            Graph<int> graph;

            for (int i = 0; i < 16; ++i)
                graph.AddNode(i);

            graph.AddEdge(12,13,diff);
           Graph<int>.Node a =  graph.FindNode(13,diff);

                */
        }
    }
}
